import styled from 'styled-components';

export const StyledTableCaption = styled.caption`
  && {
    text-align: left;
    font-weight: bold;
    font-style: normal;
    color: black;
    caption-side: top;
    margin: 0;
  }
`;
