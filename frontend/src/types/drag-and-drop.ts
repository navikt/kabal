// Values must be lowercase, setData() will make them lowercase.
export enum DragAndDropTypesEnum {
  DOCUMENT = 'document',
  JOURNALFOERT_DOCUMENT_REFERENCE = 'journalfoert-document-reference',
  JOURNALFOERT_DOCUMENT = 'journalfoert-document',
}
